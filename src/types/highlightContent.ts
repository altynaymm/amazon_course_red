export type HighlightContentProps = {

    title: string,
  buttonColor?: string,
  starColor: string,
  image?: string,
  onMoreInfoClick?: (event: React.MouseEvent<HTMLButtonElement>) => void,
  contentBackground?: string,
  backgroundImage?: string,
}